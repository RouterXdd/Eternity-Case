package eternity.classes.blocks.units;

import mindustry.world.Block;

public class PortModule extends Block {
    public int tier = 1;
    public PortModule(String name) {
        super(name);
    }
}
